export enum WebsiteUrls {
  HOME = "/",
  SOCIAL = "/social",
  REGISTRATION = "/registration",
  LOGIN = "/login",
  USED = "/used",
  NEW = "/new",
  NEW_ADN_USED = "/all",
}

export enum BaseUrl {
  URL = "http://localhost:5000/",
}

export enum Roles {
  ADMIN = "ADMIN",
  USER = "USER",
}

