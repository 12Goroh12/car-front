export enum WebsiteUrls {
  HOME = "/",
  SOCIAL = "/social",
  REGISTRATION = "/registration",
  LOGIN = "/login",
}

export enum BaseUrl {
  URL = "http://localhost:5000/",
}

export enum Roles {
  ADMIN = "ADMIN",
  USER = "USER",
}
